#pragma once

// libmpv — the embeddable playback engine that powers mpv and IINA.
// Installed via Homebrew: `brew install mpv`
// (SwiftPM locates headers/libraries through pkg-config, see Package.swift.)
#include <mpv/client.h>
#include <mpv/render.h>
